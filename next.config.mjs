/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { unoptimized: true },
  // For GitHub Pages static export, uncomment:
  // output: 'export',
  // basePath: '/Venkat-Portfolio',
  // assetPrefix: '/Venkat-Portfolio/',
};
export default nextConfig;
